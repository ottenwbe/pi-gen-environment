language: ruby
rvm:
  - 2.6.3
  - 2.5.5
  - 2.4.6

before_install:
  - gem install bundler
  - cd pi_customizer/ && bundle install && cd ..
  - cd pi_build_modifier/ && bundle install && cd ..

script:
  - rake spec